# Website (Docusaurus)

This folder contains a minimal Docusaurus site intended for GitHub Pages.

Local dev:

```bash
pnpm --dir website install
pnpm --dir website start
```

Notes:
- Keep the repo lightweight: link to CI artifacts or Releases assets for videos (don’t commit large binaries).

